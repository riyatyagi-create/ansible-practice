---
- name: vault practice
  hosts: webservers
  become: yes
  vars_files: 
    - secret.yml

  tasks: 
    - name: password print karo
      debug: 
        msg: "DB password hai: {{ db_password }}"

    - name: user print karo
      debug: 
        msg: "DB user hai: {{ db_user }}"
